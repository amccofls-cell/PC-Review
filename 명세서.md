# 의약품 심의자료 검증기 명세서 v4.0

## 1. 한 줄 요약
식약처·심평원 API 호출로 원문 세트를 받아 **두 개의 CSV**로 emit하고 → HTML이 같은 헤더로 파싱 → **11필드 카노니컬 비교** → `🟠` 의미 단위 필드만 **🤖 복사** 단추로 LLM에 위임. 기억에 의존한 답변은 폐기.

## 2. IN / OUT
**IN**: 식약처 목록·상세, 심평원 약가, 사용자 비교표(텍스트·붙여넣기), 검토 기준일, 원내 비교약 목록
**OUT**: `허가원문.csv`, `약가원문.csv`, `cache/*.json`, 비교 결과 표 + CSV 다운로드
**OUT(절대 안 함)**: 사용자 비교표 자동 수정, AI 기억 호출, 인터넷 외부 API 호출, OCR/이미지/PDF/인쇄/자체진단/다단 모달

## 3. v3.1에서 제거한 잉여
다중 샘플 6종, 자체 진단, 5-tier 카운터 패널, 카노니컬 매핑 시각화, 인쇄, OCR 4탭, 다단 modal 헤더, 3종 다운로드, 안전 알림 배너, 표방향 A/B 라디오, 병합 셀 처리, 18필드 확장 동의어 사전.

v4.0이 v3.1 대비 놓친 것을 의식적으로 청구: 처음 확인부터 직접 사용자가 비교표 한 줄 형식으로 적도록 안내하는 쪽으로 단순화.

## 4. 데이터 모델 — app.py·HTML 양쪽 문자 단위로 동일
| 카노니컬 키 | 식약처 소스 | 심평원 소스 | 비교 규칙 | LLM 위임 |
|---|---|---|---|---|
| 의약품명 | `ITEM_NAME` | `itmNm` | 완전일치 | — |
| 성분명 | `INGR_NAME`+`MAIN_ITEM_INGR` | — | 정규화일치 (sobreponer 정규화) | — |
| 제조판매사 | `ENTP_NAME` | `mnfEntpNm` | 완전일치 | — |
| 함량 | ITEM_NAME에서 추출 | — | 수치·단위 0% | — |
| 제형 | ITEM_NAME에서 추출 | — | 정규화일치 (syn dict) | — |
| 적응증 | `EE_DOC_DATA` | — | 의미 단위 | 🟠 |
| 용법용량 | `UD_DOC_DATA` | — | 의미 단위 | 🟠 |
| 소아 | `NB_DOC_DATA` 섹션 '소아에 대한 투여' | — | 의미 단위 | 🟠 |
| 보관 | `STORAGE_METHOD` | — | 완전일치 | — |
| 금기 | `NB_DOC_DATA` 섹션 '투여하지 말 것' | — | 의미 단위 | 🟠 |
| 약가 | — | `mxCprc` | 수치·단위 0% | — |

`허가원문.csv`: `의약품명, 성분명, 제조판매사, 함량, 제형, 적응증, 용법용량, 소아, 보관, 금기, BAR, 허가일자, ITEM_SEQ`
`약가원문.csv`: `의약품명, 약가, 적용시작일, 적용종료일, mdsCd, 제조판매사`

## 5. 4가지 비교 규칙
- **완전일치**: 노이즈((주), 「」, 컴마, 공백) 제거 후 `===`. 필드: 의약품명, 제조판매사, 보관.
- **정규화일치**: syn dict 적용 후 비교. 필드: 함량, 제형, 성분명. (예: `mg ↔ 밀리그램`, `정 ↔ tablet(제품명)`, `+ ↔ and`).
- **수치·단위 0%**: 단위·천단위 정규화 후 1글자 다르면 🔴. 필드: 약가, 함량.
- **의미 단위**: 자동 판정 안 함, `🟠` 고정, 🤖 복사 버튼만 노출. 필드: 적응증, 용법용량, 소아, 금기.

## 6. LLM 프롬프트 1줄 약속
```
[비교 1] 심의자료 원문: "{table_value}"
[비교 2] 공식 허가 원문: "{source_value}"
→ 판정 (✅/⚠/❌) + 핵심 차이
원문 인용 없는 답변 폐기.
```

## 7. 사용자 흐름
1. `python app.py --products "..." --demo` (키 있으면 자동 호출, 없으면 mock)
2. 같은 폴더에 `허가원문.csv` `약가원문.csv` 생성
3. `의약품_심의자료_검증기_v4.0.html` 더블클릭
4. ① 두 CSV 드래그
5. ② 기준일 + 비교표 붙여넣기 (한 줄 형식)
6. **[비교 실행]** → 🔴만 보기 체크박스 → 결과 행 옆 🤖 복사 → LLM에 Ctrl+V

## 8. 파일 구조
- app.py
- 의약품_심의자료_검증기_v4.0.html
- 명세서.md (이 파일)

## 9. API endpoint 출처
식약처/심평원 endpoint·필드값은 사전에 있던 `app.py` 원본에 명시돼 있는 값 그대로 따랐다. 환경변수 `MFDS_BASE_URL`, `HIRA_BASE_URL`로 덮어쓸 수 있게 노출한다.
